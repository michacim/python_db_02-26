def is_even(n: int) -> bool:
    """True, wenn n gerade ist, sonst False."""

def reverse_string(s: str) -> str:
    """Dreht einen String um."""

def unique_sorted(nums: list[int]) -> list[int]:
    """
    Entfernt Duplikate und sortiert aufsteigend.
    Beisp
    """

def average(nums: list[float]) -> float:
    """
    Berechnet den Durchschnitt einer Liste.
    Wirft ValueError bei leerer Liste.
    """

def normalize_whitespace(s: str) -> str:
    """
    Entfernt führende/trailing Whitespaces und reduziert mehrere Whitespaces auf eins.
    Beispiel: " hi there \n you " -> "hi there you"
    """


def is_palindrome(s: str) -> bool:
    """
    True, wenn s vorwärts == rückwärts.
    Achtung: hier ganz simpel, keine Sonderbehandlung von Leerzeichen/Case.
    """
